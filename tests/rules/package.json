{
  "name": "aguila-rules-tests",
  "private": true,
  "type": "module",
  "description": "Emulator tests for firestore.rules. Development only — the apps themselves have no dependencies.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-aguila 'node rules.test.js'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.17.1",
    "firebase-tools": "^14.0.0"
  }
}
